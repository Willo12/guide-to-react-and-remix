interface CustomComponentsProps {
  children: React.ReactNode;
}

const CustomComponents = ({ children }: CustomComponentsProps) => {
  return (
    <>
      <p>CustomComponents</p>
    </>
  );
};

export default CustomComponents;
